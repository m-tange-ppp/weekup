# デバッグ機能の削除

**完了日**: 2026-05-19
**カテゴリ**: バグ修正

## 背景・原因

`app/(tabs)/calendar.tsx` に開発中の動作確認用として追加されたデバッグパネル（任意の日付を指定して週目標・日記・KPT を作成できる UI）が残存していた。
本番リリースに向けて不要なコードを除去し、コードの保守性を向上させる必要があった。

## 対応方針

UI 層（`app/(tabs)/calendar.tsx`）に限定した削除作業。
デバッグパネルに紐づく import・ステート・ハンドラー・JSX・スタイルをすべて除去した。
`parseISO`（`loadDayData` で使用）・`getWeekEndDate`（`+ 週目標` ボタンで使用）は削除しなかった。

## 変更ファイル

| ファイル                  | 変更内容                                                                   |
| ------------------------- | -------------------------------------------------------------------------- |
| `app/(tabs)/calendar.tsx` | デバッグパネルに関連する import・ステート・ハンドラー・JSX・スタイルを削除 |
| `NOTES.md`                | `### OPEN` から `デバッグ機能の削除` を削除し、`### CLOSE` 冒頭に移動      |

## 受け入れ条件（確認済み）

- [x] カレンダー画面に「▼ デバッグ」トグルボタンが表示されない
- [x] `debugOpen`・`debugDate`・`showDebugDatePicker` ステート変数が削除されている
- [x] `onDebugDateChange` ハンドラーが削除されている
- [x] `{/* デバッグパネル */}` JSX ブロックが削除されている
- [x] デバッグ関連スタイル 9 件（`debugWrap` 等）が `StyleSheet.create` から削除されている
- [x] `DateTimePicker`・`DateTimePickerEvent`・`Platform` の import が削除されている
- [x] TypeScript コンパイルエラーなし（`npx tsc --noEmit` でエラーなし）
- [x] ESLint エラーなし（`get_errors` で確認済み）
- [x] 追加ボタン群（`+ 週目標` / `+ 日記` / `+ KPT`）が引き続き正常に存在する
- [x] 日付詳細パネル（底面スライドパネル）が引き続き正常に存在する

## 備考

特になし。
